// 
console.log ("hello Javascript");

var namee = "Ain't No Brakes";

console.log (namee);

var name = " Ismet";
    age = "24";
    gender = "Male"

console.log (name);
console.log (age);
console.log (gender);

// let message = "Good Morning";
// alert (message);
// prompt(message);
// alert (message + yourname);

let name2 = " Valentines Cake";
    layers = " 3 layers";
    flavour = " Triple Chocolate";
    price = " R200";

console.log ("Good Morning Mr Nate your" + flavour + name2 + " is ready for collection" + " it is" + layers + " so the price is" + price);

// The "use strict" directive swiches the engine to the "modern mode" mode, changng the behavour of some built-in features

// Variables are used to store this information

// A variable is a "name storage" for data. We can use variables to store data.

// To create a variable in Javascript, we need ti use the let or var keyword.

// var message

// We add a string ''
// A string in Javascript is a set of characters surrounded by quotation marks.


let message = "Good Morning";

let name = promt('Enter')



// Always remember to close a string with the same type of quotation marks you started it with.

// Operators:

// + addition
//  - subtraction
// / division
// * multiplication

// The + operator can be applied to two string values. This will join two strings together, otherwise called concatenation.

// In the console "Hey" + "there."

// Choose meaningful names
// Don't use reversed words
// Follow conventions

/* https: */
// developer.mozilla.org/en-US/docs/web/Javascript


// Up until now, all the code in our programs has been executed chronologically. LEt's enrch our code by adding conditional executions




